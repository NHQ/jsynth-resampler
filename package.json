{
  "name": "jsynth-resampler",
  "version": "0.2.0",
  "description": "resample a float32array audio buffer to whatever speed, pitch, amp, and sampleRate",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "johnnyscript",
  "license": "GFY",
  "dependencies": {
    "jbuffers": "^1.3.0",
    "pitch-shift": "0.0.0"
  }
}
